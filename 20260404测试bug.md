供应商管理界面，编辑/新建供应商 注册资本输入框标题文字修改为：注册资本（万元）

新增字典页面，选择注册证异常，应该只显示供应商对应的注册证，系统错误的显示了注册证、经营许可、营业执照全部信息

修改注册证后，注册证管理内信息已经修改，但是物质字典内引用该注册证的商品目录注册证未实时变更，需要手动点击编辑在保存。（可以在注册证管理页面加一个同步功能，修改后提示用户需要进行同步操作，完成同步后提示消失。）
结合上一条需求，在每条注册证明细内的  注册证编号  栏添加点击入口，可以弹出窗口，查看引用了该注册证的所有的物资字典明细

采购计划申请页面，申领科室应该自动识别当前登录部门（如药剂科）、申请人（如管理员、张三）

采购计划申请页面：该页面只显示订单状态为（待提交、待审核、已驳回）的订单

采购收货：确认收货/采购订单详情页面内，生产日期、失效日期输入后无法提交保存（20250127），可添加自动识别日期功能

采购订单全部前端页面、后端逻辑都需要重写，目前暂时忽略


采购入库页面和采购收货页面逻辑接反，采购收货应该只显示订单明细。
选择明细确认收货，进入到采购入库页面才可以录入效期批号信息。
未确认收货订单则保留在采购收货页面

初始化入库页面：选择物资界面商品名称丢失、单价丢失，未引用主档信息
初始化入库页面无法提交，右侧操作栏功能丢失

调拨入库：因部门无法添加，所以该功能无法测试。

新增部门：部门经理和角色设置改为非必填项；点击角色设置后界面丢失变成白屏，刷新浏览器后才恢复正常。

物资库存：将所有物资消耗出库后，物资库存里面依然有物资明细，只是变成了0；正常应该是全部消耗完之后，库存里面不再显示商品明细条目信息。


上传样本量数据：开展项目数和检测项目数（输入数字显示异常：不能为负数，实际输入为正数）

20260404
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

样本量管理：项目字典、样本量管理界面进行任何操作可以正常执行，但是后端不会保存，可能没有后端

用户账户管理：无法创建新用户，可选所属部门应该为可切换的所有部门（手术室、药剂科）
所属不同院区账号应该分别管理，或者增加显示账户所属院区，可以修改账户可管理的院区
修改账户密码后无法登录（修改后密码不生效，且原密码也失效，只能重置密码为000000）
所有账户登录应该显示自己的姓名，当前不管登录哪个号，都显示为“管理员”


在状态栏切换院区（总院区、东院区）或切换科室（急诊科、检验科）后，应该刷新浏览器状态恢复到默认（仅显示首页）

分院管理对应院区切换功能，这里显示几个分院，就可以切换几个分院，默认一个总院不可删除
部门管理对应当前院区可切换科室，并增加显示当前所属院区

采购审核：目前不管怎么操作，只能实现整单通过或整单驳回，不能部分通过或部分驳回。被驳回的明细，将会回到采购计划申请页面


采购订单查询：物资名称、采购价格列不需要，采购数量改为显示该采购单内的物资数量。

物资字典：新增字典页面内，物资编码列不再需要手动输入，由系统自动按照录入顺序自动添加。
